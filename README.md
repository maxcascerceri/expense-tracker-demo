# Expense Tracker

A small React app for logging expenses. Add an amount, category, and date; browse the full list; and keep a running total at the top.

## Run locally

```bash
npm install
npm run dev
```

Entries are saved in the browser with `localStorage`.
